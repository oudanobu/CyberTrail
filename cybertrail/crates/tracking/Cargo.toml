[package]
name = "tracking"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
domain = { path = "../domain" }
altitude = { path = "../altitude" }
tracing.workspace = true
thiserror.workspace = true
